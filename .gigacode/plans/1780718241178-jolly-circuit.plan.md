# План восстановления кодировки и добавления вкладок

## Статус
- **Цель**: Восстановить кодировку файлов и убедиться что все вкладки отображаются
- **Текущее состояние**: PlanScreen.tsx поврежден (UTF-16 LE BOM)

## Проблемы

### 1. Поврежденная кодировка PlanScreen.tsx
- Файл имеет UTF-16 LE BOM вместо UTF-8
- Содержит null-байты между символами
- Не может быть скомпилирован TypeScript

### 2. Недостающие вкладки в навигации
Проверить что все вкладки есть в App.tsx:
- ✅ Главная (home)
- ✅ Фарма (pharma)  
- ✅ Поддержка (support)
- ✅ Тренировки (training)
- ✅ Анализы (labs)
- ✅ Риски (risks)
- ✅ Питание (nutrition)
- ✅ Профиль (profile)
- ✅ Калькуляторы (calculators)

## Решение

### Шаг 1: Восстановить PlanScreen.tsx
- Скопировать содержимое из `PlanScreen_restored.tsx` в `PlanScreen.tsx`
- Убедиться что кодировка UTF-8 без BOM

### Шаг 2: Проверить другие файлы
- Проверить кодировку всех screen-файлов
- Убедиться что все файлы в UTF-8

### Шаг 3: Разделить большие файлы (если нужно)
- Если PlanScreen слишком большой, разделить на:
  - PlanScreen.tsx (главный компонент)
  - PlanTraining.tsx (тренерская часть)
  - PlanCycles.tsx (циклическая часть)
  - PlanRationale.tsx (обоснование)

### Шаг 4: Проверить компиляцию
```bash
npx tsc --noEmit
npx vite build
```

## Файлы для редактирования
1. `D:\V9\src\ui\screens\PlanScreen.tsx` - восстановить из резервной копии
2. `D:\V9\src\App.tsx` - проверить навигацию (уже в порядке)

## Проверка
- Все 9 вкладок отображаются в навигации
- PlanScreen.tsx загружается без ошибок
- TypeScript компилируется без ошибок
- Приложение строится успешно
